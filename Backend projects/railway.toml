[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python secure-proxy-server.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[build.nixpacksConfig]
packages = ["python311"]
